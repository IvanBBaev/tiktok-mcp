#!/usr/bin/env node
/**
 * tiktok-mcp-ai — process entry point (scaffold placeholder).
 *
 * The real bootstrap (env load, transport selection between stdio and
 * Streamable HTTP, MCP server construction, tool registration, CLI subcommand
 * dispatch for `login`/`doctor`) lands with the MCP-server and CLI tasks. This
 * placeholder exists only so the toolchain has a compilable entry module and
 * `npm run check` is green on the bare scaffold.
 *
 * See docs/ARCHITECTURE.md § 2 (Bootstrap) for the target design.
 */
export {};
